{
  "name": "middleware_access-control",
  "version": "1.0.0",
  "description": "Access control middleware acting as forward auth for Traefik",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "middleware",
    "authentication",
    "node.js",
    "express.js",
    "jwt"
  ],
  "author": "DansMonGosier inc.",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^8.43.0",
    "eslint-config-google": "^0.14.0"
  }
}
